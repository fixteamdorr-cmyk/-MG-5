# دليل نشر تطبيق 2go على متجر Google Play

لقد قمت بإعداد البنية التحتية للتطبيق باستخدام **Capacitor**. اتبع الخطوات التالية لتحويل هذا المشروع إلى ملف `APK` أو `AAB` جاهز للمتجر.

## 1. المتطلبات قبل البدء
* تأكد من تنصيب **Node.js** و **Android Studio** على جهازك الشخصي.
* امتلاك حساب مطور في Google Play Console (بتكلفة 25$).

## 2. الخطوات البرمجية (على جهازك)

بعد تحميل المشروع (Export to ZIP) وفك الضغط، افتح نافذة الأوامر (Terminal) داخل مجلد المشروع وقم بتنفيذ ما يلي:

### أ. بناء نسخة الويب
```bash
npm install
npm run build
```

### ب. إضافة منصة الأندرويد
```bash
npx cap add android
```

### ج. مزامنة الكود مع الأندرويد
كلما قمت بتعديل في كود React، نفذ هذا الأمر:
```bash
npm run build
npx cap sync
```

### د. فتح المشروع في Android Studio
```bash
npx cap open android
```

---

## 3. الخطوات داخل Android Studio لأول مرة

1. انتظر حتى ينتهي التحميل (Gradle Sync).
2. اذهب إلى قائمة **Build** > **Generate Signed Bundle / APK**.
3. اختر **Android App Bundle** (للمتجر) أو **APK** (للتجربة الشخصية).
4. اتبع خطوات إنشاء "Key Store" (احتفظ بهذا الملف وكلمة المرور في مكان آمن جداً، بدونه لن تستطيع تحديث التطبيق لاحقاً).
5. الملف الناتج سيكون بامتداد `.aab` في مجلد `android/app/release`.

---

## 4. الرفع على المتجر (Google Play Console)

1. أنشئ تطبيقاً جديداً في لوحة تحكم جوجل.
2. اتبع خطوات "App Setup" (العمر، الخصوصية، تصنيف المحتوى).
3. في قسم **Production**، قم بإنشاء "New Release" وارفع ملف الـ `.aab`.
4. أرسل التطبيق للمراجعة (تستغرق عادة من 3 إلى 7 أيام).

---

## نصائح هامة
* **الأيقونات:** يمكنك تغيير أيقونات التطبيق بسهولة عبر استبدال الصور في مجلد `android/app/src/main/res`.
* **اسم الحزمة:** إذا أردت تغيير `com.fixteam.togo` لاحقاً، يجب تغييره في ملف `capacitor.config.ts` وفي إعدادات الأندرويد.
