import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fixteam.togo',
  appName: '2go Sfax',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
